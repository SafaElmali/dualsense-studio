{
  "name": "dualsense-controller",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive PS5 DualSense recreation built with Three.js.",
  "type": "module",
  "scripts": {
    "test": "npm run build && node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "test:seo": "npm run build && node --test tests/seo.test.mjs",
    "streamers:review": "node scripts/review-streamers.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.3"
  }
}
